# @arichtext/editor-js

Optional, diagnostic Editor.js OutputData adapter. It supports paragraph, header,
quote, code, delimiter and list blocks. Unsupported tools are reported as losses;
Editor.js plugin-specific data is not silently treated as ART.
