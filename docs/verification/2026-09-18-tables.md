# Markdown pipe tables: local verification

Runtime/test revision: `343395960239c471cd07d25e4d2c3701ef0f6a35`.
This revision includes draft increments #96–#99.

- `pnpm verify`: package policy, builds, TypeScript/Vue checks, 1,061 unit tests and bundle ceilings passed. One further table regression was then included in the final unit run below; runtime code was unchanged.
- Final `pnpm test`: all 1,062 unit tests across 86 files passed, including the additional separator-like body-row regression.
- `node scripts/build-distribution.mjs`: browser distribution and showcase rebuilt.
- `pnpm exec playwright test tests/browser/markdown-compatibility.spec.ts tests/browser/live-source.spec.ts tests/browser/profiles.spec.ts tests/browser/autolink.spec.ts --workers=2 --retries=0`: all 150 targeted cases passed, 30 per profile; no failures, retries or skips. Runtime/build/browser-test files remained fixed throughout the run.
- `pnpm verify:distribution`: 27 tarballs inspected; SSR exports and isolated TypeScript consumer passed.
- Minimal/standard gzip: 40,430/59,313 bytes, within ceilings. No dependency or schema changes.

All eight published GFM table examples (198–205) are tracked unchanged and match
retained ART semantics after HTML import. Header roles, column alignment and HTML
table-section wrappers are not represented in ART. The paragraph soft break in
example 203 folds to a space. No byte-identical HTML claim is made.

Initial regressions reproduced thirteen failures. Tests now cover ragged body
rows, header/delimiter width matching, escaped pipes inside code and text,
backslash runs, Unicode cell content, escaped row endings, recognized block
boundaries and a 65,536-cell automatic-padding limit that leaves remaining input
for ordinary parsing. Browser tests verify ragged-row rendering and code-cell
source/visual/reimport round trips.

See [compatibility scope](../markdown-compatibility.md) and
[machine-readable evidence](2026-09-18-tables.json). This is targeted browser
evidence; the [last full baseline](2026-09-16-surfaces.md) remains 470 cases.
No full CommonMark/GFM conformance, physical-device/IME or screen-reader
verification is claimed. No GitHub Actions or public release. Existing
documentation screenshots were not regenerated.
