# Showcase profiles and editor surfaces: local verification

Runtime/test revision: `e3ef33ddbba388aa4fc51221824ede8ab0858306`.

- `pnpm verify`: 648 unit tests across 78 files, builds, TypeScript/Vue, package policy and bundle ceilings passed.
- `node scripts/build-distribution.mjs`: browser distribution rebuilt; an HTTP fetch confirmed port 8080 serves the new showcase HTML.
- Initial focused showcase, preset and live-source browser run: 75 cases across five profiles passed. Final custom-toolbar/find-and-replace regressions: 40 cases across five profiles passed.
- `pnpm exec playwright test --workers=2 --retries=0`: all 470 cases (94 per browser profile) passed, zero failures/retries/skips. Runtime/build files remained fixed during this run.
- `pnpm verify:distribution`: 27 offline tarballs, SSR export imports and isolated TypeScript consumer passed.
- Minimal/standard gzip: 38,740/57,633 bytes, within ceilings.

An earlier four-worker run was interrupted after failures: a reproducible source-profile switch left Find and Replace open, while other cases timed out or failed a gallery-position check. A new unit regression failed before the profile cleanup fix and passed afterward; all five focused browser regressions passed after rebuilding. A subsequent two-worker run exposed a custom-toolbar selection race, reproduced by moving focus before selectionchange delivery. Capturing selection after keyboard input and on editor blur fixes it, with a deterministic browser regression. The final full run uses two workers with unchanged test deadlines. No interrupted/failed run is counted as passing evidence.

New browser regressions cover source/output profile independence and every installed output converter, explicit reported-loss acceptance, consistent appearance widths and source/visual spacing, no textarea focus outline, and an opaque top-layer inline toolbar above a high-z-index sibling inside a clipped/transformed ancestor. The desktop showcase was also visually inspected. Existing documentation screenshots were not regenerated in this run.

[Machine-readable evidence](2026-09-16-surfaces.json). All checks ran locally; GitHub Actions remains disabled. Physical-device, IME, screen-reader and release gates remain open. Source remains a plain-text textarea for incomplete drafts; it shares editor chrome and spacing rather than rendering source markup.
