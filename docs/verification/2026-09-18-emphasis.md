# Markdown emphasis delimiter runs: local verification

Runtime/test revision: `7f9c4ab9683e9416537bcfc0a53a71143261f57c`.
This revision includes the preceding thematic-break and autolink increments.

- `pnpm verify`: 999 unit tests across 84 files, package policy, builds, TypeScript/Vue checks and bundle ceilings passed.
- `node scripts/build-distribution.mjs`: browser distribution and showcase rebuilt.
- `pnpm exec playwright test tests/browser/markdown-compatibility.spec.ts tests/browser/live-source.spec.ts tests/browser/profiles.spec.ts tests/browser/autolink.spec.ts --workers=2 --retries=0`: all 130 targeted cases passed, 26 per profile; no failures, retries or skips. Runtime/build/test files remained fixed throughout the final run.
- `pnpm verify:distribution`: 27 tarballs inspected; SSR exports and isolated TypeScript consumer passed.
- Minimal/standard gzip: 40,322/59,192 bytes, within ceilings. No dependency or schema changes.

All 132 upstream CommonMark 0.31.2 emphasis examples are tracked unchanged.
129 match canonical ART semantics; three raw-HTML interactions remain explicit
mismatches. The oracle imports upstream HTML into ART: repeated marks collapse,
nesting order becomes canonical and soft breaks fold to spaces. This does not
assert byte-identical HTML. Previously excluded examples 14 and 55 now match.

Tests cover Unicode flanking, intraword underscores, nested delimiter runs,
escaped markers, code/link boundaries and long unmatched sequences. A browser
round-trip regression exposed ambiguous export runs around mixed italic and code
spans; shared outer marks now stay open across adjacent text nodes. The final
run above includes that fix and preserves normalized link destinations.

See [compatibility scope](../markdown-compatibility.md) and
[machine-readable evidence](2026-09-18-emphasis.json). This is targeted browser
evidence; the [last full baseline](2026-09-16-surfaces.md) remains 470 cases.
No full CommonMark/GFM conformance, physical-device/IME or screen-reader
verification is claimed. No GitHub Actions or public release. Existing
documentation screenshots were not regenerated.
