# Local verification — 2026-09-15

## Tested source

Branch: `feat/v01-editing-primitives`, continuing `f30b4688e35c581f095770c434e748c40a94c23c` with the local changes described below. These results apply to the working implementation, not to the unchanged remote commit or the earlier stacked PR heads.

Environment: macOS 15.7.9 (24G830), Node 26.8.1, pnpm 10.17.1, TypeScript 7.0.2, Vitest 5.0.1, Playwright 1.63.0, esbuild 0.28.2.

Lockfile SHA-256: `4da7075023fade55790c239908989fdcbbdab79a45c32f1f14099fd72fb745dd`.

Implementation SHA-256: `2264585df9196af74720d86adf465fe27670178b171bf214d01465fa038badea`. Fingerprint input is the sorted, non-ignored file set in `packages/`, `scripts/`, `tests/`, plus root `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `tsconfig.json`, `vitest.config.ts` and `playwright.config.ts`; each entry is UTF-8 path, NUL, file bytes, NUL.

## Results

| Check | Result |
|---|---|
| `pnpm install --frozen-lockfile` | Passed without lockfile changes |
| `pnpm verify` | Passed |
| Package policy | 25 packages passed |
| Build and typecheck | All 25 packages passed |
| Unit/regression tests | 310 passed across 56 files |
| Minimal bundle | 19,914 bytes gzip / 51,200-byte ceiling |
| Standard toolbar bundle | 25,031 bytes gzip / 66,560-byte ceiling |
| `pnpm exec playwright test --retries=0 --workers=2` | 40 passed; 0 failed, skipped or flaky |
| `git diff --check` | Passed |

Browser versions: Chromium 153.0.8010.12, Firefox 155.0, WebKit 26.6. Each desktop target and the Pixel 7 Chromium/iPhone 15 WebKit emulation profiles passed all eight tests. Local artifacts are `test-results/playwright-results.json` and `test-results/local-verify.log` (ignored generated files).

The full local workflow is now available as `pnpm verify:local`, combining the two verified gates. No GitHub Actions run was used for this validation.

## Changes covered

- Completed list item Enter/empty-item exit commands and the optional `enableListEditing()` adapter, with annotation mapping, task state, ordered numbering, input cancellation and atomic undo/redo.
- Fixed the package-policy initialization crash, missing workspace dependencies and TypeScript build errors.
- Added the lockfile and root imports needed by browser fixtures and bundle measurements.
- Coalesced identical adjacent text runs after block joins and fragment insertion.
- Made the upload-race regression wait for upload start rather than one assumed microtask.
- Fixed selection reads across shadow boundaries and container endpoints. Real browser typing previously reversed characters through native fallback; toolbar actions lacked a logical selection.
- Forwarded external labels/descriptions using element references. Chromium's native accessibility tree confirms the editable textbox is named “Body”; Firefox/WebKit verify the reflected references and textbox semantics.

## Limits

This is local development evidence, not production certification. Physical phones, screen readers, real IME input, RTL/mobile keyboards and public package tarball inspection remain outstanding. The mobile profiles use browser emulation and keyboard automation. Other Node/OS versions were not tested.

The unit DOM environment blocks external scripts/styles/iframe loading. The malicious-iframe fixture emits an expected Happy DOM “iframe page loading is disabled” diagnostic; the suite exits successfully and its sanitization assertions pass.

Remaining editing scope is documented in [editing primitives](../editing-primitives.md): collapsed-caret links and Mod+K, complex list-item behavior, indent/outdent, list-boundary Backspace and table cell navigation. No PR was merged and no remote project status was changed by this local continuation.

## Integration follow-up

The changes were committed as `3eb74e4`, followed by `96aed3a` removing the hosted workflow. A fresh detached checkout of `96aed3a` passed `pnpm install --frozen-lockfile` and the complete `pnpm verify:local` command: 310 unit tests, 40 browser tests, all package checks and the same measured bundle sizes. Browser results had zero failures, skips or flaky retries. This clean-checkout run validates the combined branch history for integration into `main`; the remaining limits above still apply.
