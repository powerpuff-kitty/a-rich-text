# Setext headings: local verification

Runtime/test revision: `3ead2e9c7bf80a3d5f56244c7aae049d597b56d1`.

- `pnpm verify`: 683 unit tests across 79 files, package policy, builds, TypeScript/Vue checks and bundle ceilings passed.
- `node scripts/build-distribution.mjs`: browser distribution rebuilt, including the local showcase.
- `pnpm exec playwright test tests/browser/markdown-compatibility.spec.ts tests/browser/live-source.spec.ts tests/browser/profiles.spec.ts --workers=2 --retries=0`: 75 targeted cases across five profiles passed; zero failures, retries or skips. Runtime and build files stayed fixed throughout.
- `pnpm verify:distribution`: 27 tarballs inspected; SSR exports and isolated TypeScript consumer passed.
- Minimal/standard gzip: 38,789/57,684 bytes, within ceilings. No dependency changes.

All 123 Markdown unit tests pass. All 27 upstream setext examples are accounted for: 24 match under the documented normalization, while lazy blockquote continuation (93) and tight-list HTML wrappers (94/99) remain explicit mismatches. Indented-code example 115 now matches, raising that section to 11 of 12.

This is targeted browser evidence; the [last full baseline](2026-09-16-surfaces.md) remains 470 cases. Full CommonMark/GFM conformance, physical-device/IME/screen-reader testing and release gates remain open. No GitHub CI or public release.
